<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Material Dark-->
    <style name="Theme.Material" parent="@android:style/Theme.Material">
        <item name="android:statusBarColor">?android:attr/colorPrimaryDark</item>
        <item name="android:navigationBarColor">?android:attr/colorPrimaryDark</item>
        <item name="android:listDivider">@*common:drawable/list_divider_cyngn</item>
    </style>

    <!-- Material Light-->
    <style name="Theme.Material.Light" parent="@android:style/Theme.Material.Light">
        <item name="android:statusBarColor">?android:attr/colorPrimaryDark</item>
        <item name="android:navigationBarColor">?android:attr/colorPrimaryDark</item>
        <item name="android:listDivider">@*common:drawable/list_divider_cyngn</item>
    </style>

    <!-- Holo2Material Dark -->
    <style name="Theme.Holo" parent="@style/Theme.Material"/>

    <!-- Holo2Material Light -->
    <style name="Theme.Holo.Light" parent="@style/Theme.Material.Light"/>
</resources>